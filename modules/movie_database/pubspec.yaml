name: movie_database
description: Local database entities for the YaMovies lecture app.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.12.2

dependencies:
  drift: ^2.0.0
  drift_flutter: ^0.2.4
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner: ^2.4.0
  drift_dev: ^2.0.0
